import { EmployeepipexamplePipe } from './employeepipexample.pipe';

describe('EmployeepipexamplePipe', () => {
  it('create an instance', () => {
    const pipe = new EmployeepipexamplePipe();
    expect(pipe).toBeTruthy();
  });
});
