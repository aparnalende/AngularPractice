import { CaldiscountPipe } from './caldiscount.pipe';

describe('CaldiscountPipe', () => {
  it('create an instance', () => {
    const pipe = new CaldiscountPipe();
    expect(pipe).toBeTruthy();
  });
});
